version: "2"
linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - database/sql.(*DB).Close
        - (*os.File).Close
        - io.Closer.Close
run:
  tests: false
